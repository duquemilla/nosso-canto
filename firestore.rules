rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Casal Nosso Canto: dados compartilhados seguros do app
    match /coupleData/{document=**} {
      allow read, write: if true;
    }
  }
}
